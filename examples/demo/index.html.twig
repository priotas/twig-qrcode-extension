<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/foundation/6.2.3/foundation.css" />

    <style>
        svg rect {
            fill: lime;
        }
    </style>

</head>

<body>

    <div class="row">
        <h3>Simple example</h3>

        <img src="{{ 'http://kewl.example.com' | qrcode(size=100) }}" />

        <hr />

        <h3>EPC example</h3>

        {% set EPC = 'BCD\n001\n1\nSCT\nBYLADEM1001\nMax Mustermann\nDE02120300000000202051\nEUR100,00\n\n\n\n' %}
        <img src="{{ EPC | qrcode(size=200) }}" />

        <hr />

        <h3>Inline SVG</h3>

        {{ 'http://kewl.example.com' | qrcode(size=200,type="svg",svg="inline") }}

        <hr />

        <h3>URL with label</h3>

        <form method="post">
            <input value="{{ url }}" type="url" name="url" placeholder="Enter a valid url">
            <input type="submit" value="Submit" class="button">
        </form>

        {% if url %}
        <img src="{{ url | qrcode(size=20, label=url, type="png") }}" />
        {% endif %}
    </div>
</body>

</html>